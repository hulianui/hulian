# 瑚琏 Hulian P0+P1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建成瑚琏设计系统的脊柱与展示基建——一个 pnpm+Turborepo monorepo，含 `@hulianui/tokens`（OKLCH 明暗 token + Tailwind v4 preset）、`@hulianui/ui`（ThemeProvider + Button/Switch/Dialog 三标杆，Base UI headless）、`@hulianui/mocks`（faker + MSW）、`apps/www`（Next.js 文档站，5512，dogfood 模型 A，四种 mock 展示）。

**Architecture:** 分发模型 A——`@hulianui/ui` 发源码+className，消费方（www 为首个 dogfood）用 Tailwind v4 + 瑚琏 preset 扫源码出样式。主题走两层 CSS 变量（原始 OKLCH 层 + 语义层），切 `[data-theme]` 0 闪烁。ThemeProvider 框架无关 context 留库内，anti-FOUC inline script 归 www root layout。

**Tech Stack:** pnpm 8 workspace + Turborepo · TypeScript · Tailwind CSS v4 · @base-ui-components/react · class-variance-authority + clsx + tailwind-merge · lucide-react · Next.js 15 (App Router) + MDX · @faker-js/faker · msw · Vitest（单元）· Playwright/截图（视觉验收）

**端口：** www dev = 5512（写死 `next dev -p 5512`）。MSW 浏览器内不占端口。

---

## 文件结构总览

```
hulian/
├── pnpm-workspace.yaml
├── package.json                      # 根：scripts(dev/build/typecheck/test) + turbo + devDeps
├── turbo.json
├── tsconfig.base.json
├── .npmrc                            # auto-install-peers=false（库的 peer 不自动装进库）
├── packages/
│   ├── tokens/
│   │   ├── package.json              # @hulianui/tokens, exports tokens.css + preset
│   │   ├── src/primitives.css        # OKLCH 原始色板（:root，明暗共用）
│   │   ├── src/semantic.css          # 语义层：:root(light) + [data-theme=dark]
│   │   ├── src/tokens.css            # @import primitives + semantic（消费方引这一个）
│   │   └── src/preset.css            # Tailwind v4 @theme：语义 token → 工具类
│   ├── ui/
│   │   ├── package.json              # @hulianui/ui, peerDeps: react/react-dom/tailwindcss/@base-ui-components/react
│   │   ├── tsconfig.json
│   │   ├── src/lib/cn.ts             # clsx + tailwind-merge
│   │   ├── src/theme/theme-provider.tsx   # 框架无关 context（storage/system/data-theme）
│   │   ├── src/theme/use-theme.ts
│   │   ├── src/button/{button.tsx,button.types.ts,button.showcase.tsx,index.ts}
│   │   ├── src/switch/{switch.tsx,switch.types.ts,switch.showcase.tsx,index.ts}
│   │   ├── src/dialog/{dialog.tsx,dialog.types.ts,dialog.showcase.tsx,index.ts}
│   │   ├── src/showcase/types.ts     # ControlSchema / StateSpec 公共类型（showcase 约定）
│   │   └── src/index.ts              # 桶导出
│   └── mocks/
│       ├── package.json              # @hulianui/mocks
│       ├── src/factories.ts          # faker 数据工厂（user/tableRows）
│       ├── src/handlers.ts           # MSW handlers
│       ├── src/browser.ts            # setupWorker
│       └── src/index.ts
└── apps/www/
    ├── package.json                  # next dev -p 5512
    ├── next.config.mjs               # MDX
    ├── tsconfig.json
    ├── app/globals.css               # @import tokens.css + preset + @source 扫 @hulianui/ui
    ├── app/layout.tsx                # anti-FOUC inline script + tokens 最前 + ThemeProvider
    ├── app/theme-script.tsx          # inline script 字符串
    ├── app/page.tsx                  # 首页 + 三组件入口
    ├── app/components/[slug]/...     # 组件展示页
    ├── components/theme-toggle.tsx
    ├── components/showcase/component-preview.tsx
    ├── components/showcase/states-gallery.tsx
    ├── components/showcase/playground.tsx
    └── components/msw-provider.tsx   # App Router 客户端启动 MSW worker
```

---

## P0 — 脊柱

### Task 1: Monorepo 骨架

**Files:** Create `pnpm-workspace.yaml`, `package.json`, `turbo.json`, `tsconfig.base.json`, `.npmrc`

- [ ] **Step 1: 写 workspace + 根配置**

`pnpm-workspace.yaml`:
```yaml
packages:
  - "packages/*"
  - "apps/*"
```

`.npmrc`:
```
auto-install-peers=true
```
（根装方便；库 package.json 仍把框架列 peer，发布契约不受 .npmrc 影响。）

根 `package.json`:
```json
{
  "name": "hulian",
  "private": true,
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
```

`turbo.json`:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": { "cache": false, "persistent": true },
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] },
    "typecheck": { "dependsOn": ["^build"] },
    "test": {}
  }
}
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022", "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext", "moduleResolution": "Bundler",
    "jsx": "react-jsx", "strict": true, "skipLibCheck": true,
    "esModuleInterop": true, "resolveJsonModule": true,
    "declaration": true, "noEmit": true
  }
}
```

- [ ] **Step 2: 验证** Run: `pnpm install` then `pnpm exec turbo --version`. Expected: 安装成功、turbo 版本输出。
- [ ] **Step 3: Commit** `git add -A && git commit -m "chore: monorepo 骨架(pnpm workspace + turbo)"`

---

### Task 2: @hulianui/tokens — OKLCH 明暗 token + Tailwind v4 preset

**Files:** Create `packages/tokens/{package.json,src/primitives.css,src/semantic.css,src/tokens.css,src/preset.css}`

- [ ] **Step 1: package.json**
```json
{
  "name": "@hulianui/tokens",
  "version": "0.0.0",
  "exports": {
    "./tokens.css": "./src/tokens.css",
    "./preset.css": "./src/preset.css",
    "./primitives.css": "./src/primitives.css",
    "./semantic.css": "./src/semantic.css"
  },
  "files": ["src"]
}
```

- [ ] **Step 2: primitives.css（OKLCH 色板，明暗共用，不随主题变）**
```css
:root {
  --gray-50: oklch(0.985 0 0);
  --gray-100: oklch(0.967 0.001 286);
  --gray-200: oklch(0.92 0.004 286);
  --gray-300: oklch(0.871 0.006 286);
  --gray-400: oklch(0.705 0.015 286);
  --gray-500: oklch(0.552 0.016 286);
  --gray-600: oklch(0.442 0.017 286);
  --gray-700: oklch(0.37 0.013 286);
  --gray-800: oklch(0.274 0.006 286);
  --gray-900: oklch(0.21 0.006 286);
  --gray-950: oklch(0.141 0.005 286);
  --brand-400: oklch(0.72 0.16 250);
  --brand-500: oklch(0.62 0.19 255);
  --brand-600: oklch(0.54 0.20 258);
  --danger-500: oklch(0.62 0.22 25);
  --danger-600: oklch(0.55 0.22 25);
  --success-500: oklch(0.7 0.16 155);
  --white: oklch(1 0 0);
  --black: oklch(0 0 0);
}
```

- [ ] **Step 3: semantic.css（语义层，明暗在此切）**
```css
:root {
  --color-bg: var(--gray-50);
  --color-surface: var(--white);
  --color-surface-hover: var(--gray-100);
  --color-foreground: var(--gray-900);
  --color-muted: var(--gray-500);
  --color-border: var(--gray-200);
  --color-ring: var(--brand-500);
  --color-primary: var(--brand-600);
  --color-primary-foreground: var(--white);
  --color-primary-hover: var(--brand-500);
  --color-danger: var(--danger-600);
  --color-danger-foreground: var(--white);
  --radius: 0.625rem;
}
[data-theme="dark"] {
  --color-bg: var(--gray-950);
  --color-surface: var(--gray-900);
  --color-surface-hover: var(--gray-800);
  --color-foreground: var(--gray-50);
  --color-muted: var(--gray-400);
  --color-border: var(--gray-800);
  --color-ring: var(--brand-400);
  --color-primary: var(--brand-500);
  --color-primary-foreground: var(--gray-950);
  --color-primary-hover: var(--brand-400);
  --color-danger: var(--danger-500);
  --color-danger-foreground: var(--gray-950);
}
```

- [ ] **Step 4: tokens.css（消费方引这一个）**
```css
@import "./primitives.css";
@import "./semantic.css";
```

- [ ] **Step 5: preset.css（Tailwind v4 @theme：语义 token → 工具类 + dark variant 绑 data-theme）**
```css
@import "tailwindcss";
@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));
@theme inline {
  --color-bg: var(--color-bg);
  --color-surface: var(--color-surface);
  --color-surface-hover: var(--color-surface-hover);
  --color-foreground: var(--color-foreground);
  --color-muted: var(--color-muted);
  --color-border: var(--color-border);
  --color-ring: var(--color-ring);
  --color-primary: var(--color-primary);
  --color-primary-foreground: var(--color-primary-foreground);
  --color-primary-hover: var(--color-primary-hover);
  --color-danger: var(--color-danger);
  --color-danger-foreground: var(--color-danger-foreground);
  --radius: var(--radius);
}
```
> 注：套 skill `tailwind-v4-shadcn-dark-variant-data-theme-bridge` 的写法——dark variant 用 `@custom-variant` 绑到 `[data-theme="dark"]`，而非默认 `prefers-color-scheme`。

- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(tokens): OKLCH 明暗两层 token + Tailwind v4 preset"`

---

### Task 3: @hulianui/ui 包骨架 + cn 工具

**Files:** Create `packages/ui/{package.json,tsconfig.json,src/lib/cn.ts,src/showcase/types.ts}`; test `packages/ui/src/lib/cn.test.ts`

- [ ] **Step 1: package.json（peerDeps 契约）**
```json
{
  "name": "@hulianui/ui",
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "files": ["src"],
  "peerDependencies": {
    "react": ">=18", "react-dom": ">=18",
    "tailwindcss": ">=4", "@base-ui-components/react": ">=1.0.0-beta"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.0", "clsx": "^2.1.0",
    "tailwind-merge": "^2.5.0", "lucide-react": "^0.400.0"
  },
  "devDependencies": {
    "react": "^18.3.0", "react-dom": "^18.3.0",
    "@base-ui-components/react": "latest",
    "vitest": "^2.0.0", "@testing-library/react": "^16.0.0",
    "jsdom": "^25.0.0", "@vitejs/plugin-react": "^4.3.0"
  },
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" }
}
```
> 实测时若 `@base-ui-components/react` 版本号需调整，以 `npm view @base-ui-components/react version` 为准。

- [ ] **Step 2: cn.ts**
```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 3: cn.test.ts（失败先行）**
```ts
import { describe, it, expect } from "vitest";
import { cn } from "./cn";
describe("cn", () => {
  it("merges conflicting tailwind classes, last wins", () => {
    expect(cn("px-2", "px-4")).toBe("px-4");
  });
  it("drops falsy", () => {
    expect(cn("a", false, null, undefined, "b")).toBe("a b");
  });
});
```

- [ ] **Step 4: showcase/types.ts（四件套约定的公共类型）**
```ts
import type { ReactNode } from "react";
export type ControlType = "text" | "select" | "boolean" | "number";
export interface Control {
  prop: string;
  type: ControlType;
  options?: string[];           // for select
  defaultValue: string | number | boolean;
  label?: string;
}
export interface StateSpec {
  name: string;                 // default/hover/disabled/...
  render: () => ReactNode;
}
export interface ShowcaseSpec {
  controls: Control[];
  states: StateSpec[];
  renderWithProps: (props: Record<string, unknown>) => ReactNode;
  toCode: (props: Record<string, unknown>) => string;
}
```

- [ ] **Step 5: vitest config** Create `packages/ui/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
export default defineConfig({
  plugins: [react()],
  test: { environment: "jsdom", globals: true },
});
```

- [ ] **Step 6: Run tests** Run: `pnpm --filter @hulianui/ui test`. Expected: cn 2 tests PASS.
- [ ] **Step 7: Commit** `git add -A && git commit -m "feat(ui): 包骨架 + cn 工具 + showcase 约定类型"`

---

### Task 4: ThemeProvider（框架无关 context）

**Files:** Create `packages/ui/src/theme/{theme-provider.tsx,use-theme.ts}`; test `theme-provider.test.tsx`

- [ ] **Step 1: use-theme.ts（context 定义）**
```tsx
"use client";
import { createContext, useContext } from "react";
export type Theme = "light" | "dark";
export type ThemeSetting = Theme | "system";
export interface ThemeContextValue {
  theme: Theme;              // 解析后的实际主题
  setting: ThemeSetting;     // 用户选择（含 system）
  setTheme: (s: ThemeSetting) => void;
  toggle: () => void;
}
export const ThemeContext = createContext<ThemeContextValue | null>(null);
export function useTheme() {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used within ThemeProvider");
  return ctx;
}
```

- [ ] **Step 2: theme-provider.tsx（storage + system + data-theme，无 Next 依赖）**
```tsx
"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { ThemeContext, type Theme, type ThemeSetting } from "./use-theme";

const STORAGE_KEY = "hulian-theme";

function systemTheme(): Theme {
  if (typeof window === "undefined") return "light";
  return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
}
function resolve(setting: ThemeSetting): Theme {
  return setting === "system" ? systemTheme() : setting;
}

export function ThemeProvider({
  children, defaultSetting = "system",
}: { children: React.ReactNode; defaultSetting?: ThemeSetting }) {
  const [setting, setSettingState] = useState<ThemeSetting>(defaultSetting);
  const [theme, setThemeState] = useState<Theme>(() => resolve(defaultSetting));

  // hydrate from storage once on mount
  useEffect(() => {
    const stored = window.localStorage.getItem(STORAGE_KEY) as ThemeSetting | null;
    const s = stored ?? defaultSetting;
    setSettingState(s);
    setThemeState(resolve(s));
  }, [defaultSetting]);

  // reflect to <html data-theme> + react to system changes when setting=system
  useEffect(() => {
    const t = resolve(setting);
    setThemeState(t);
    document.documentElement.setAttribute("data-theme", t);
    if (setting !== "system") return;
    const mq = window.matchMedia("(prefers-color-scheme: dark)");
    const onChange = () => {
      const next = mq.matches ? "dark" : "light";
      setThemeState(next);
      document.documentElement.setAttribute("data-theme", next);
    };
    mq.addEventListener("change", onChange);
    return () => mq.removeEventListener("change", onChange);
  }, [setting]);

  const setTheme = useCallback((s: ThemeSetting) => {
    window.localStorage.setItem(STORAGE_KEY, s);
    setSettingState(s);
  }, []);
  const toggle = useCallback(() => {
    setTheme(resolve(setting) === "dark" ? "light" : "dark");
  }, [setting, setTheme]);

  const value = useMemo(() => ({ theme, setting, setTheme, toggle }), [theme, setting, setTheme, toggle]);
  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}
```

- [ ] **Step 3: theme-provider.test.tsx**
```tsx
import { describe, it, expect, beforeEach } from "vitest";
import { render, screen, act } from "@testing-library/react";
import { ThemeProvider } from "./theme-provider";
import { useTheme } from "./use-theme";

function Probe() {
  const { theme, toggle } = useTheme();
  return <button data-theme-value={theme} onClick={toggle}>t</button>;
}
beforeEach(() => { localStorage.clear(); document.documentElement.removeAttribute("data-theme"); });

describe("ThemeProvider", () => {
  it("sets data-theme on html and toggles", async () => {
    render(<ThemeProvider defaultSetting="light"><Probe /></ThemeProvider>);
    expect(document.documentElement.getAttribute("data-theme")).toBe("light");
    await act(async () => { screen.getByText("t").click(); });
    expect(document.documentElement.getAttribute("data-theme")).toBe("dark");
    expect(localStorage.getItem("hulian-theme")).toBe("dark");
  });
});
```

- [ ] **Step 4: Run** `pnpm --filter @hulianui/ui test`. Expected: PASS.
- [ ] **Step 5: Commit** `git add -A && git commit -m "feat(ui): 框架无关 ThemeProvider(storage+system+data-theme)"`

---

### Task 5: Button（CVA + 语义 token）

**Files:** Create `packages/ui/src/button/{button.tsx,button.types.ts,button.showcase.tsx,index.ts}`; test `button.test.tsx`

- [ ] **Step 1: button.types.ts**
```ts
import type { VariantProps } from "class-variance-authority";
import type { buttonVariants } from "./button";
import type { ButtonHTMLAttributes } from "react";
export interface ButtonProps
  extends ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  loading?: boolean;
}
```

- [ ] **Step 2: button.tsx（只消费语义 token 工具类，焦点环用 ring token）**
```tsx
import { forwardRef } from "react";
import { cva } from "class-variance-authority";
import { Loader2 } from "lucide-react";
import { cn } from "../lib/cn";
import type { ButtonProps } from "./button.types";

export const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 rounded-[var(--radius)] font-medium transition-colors outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-bg disabled:opacity-50 disabled:pointer-events-none",
  {
    variants: {
      variant: {
        solid: "bg-primary text-primary-foreground hover:bg-primary-hover",
        outline: "border border-border bg-surface text-foreground hover:bg-surface-hover",
        ghost: "text-foreground hover:bg-surface-hover",
      },
      tone: { brand: "", danger: "" },
      size: { sm: "h-8 px-3 text-sm", md: "h-10 px-4 text-sm", lg: "h-12 px-6 text-base" },
    },
    compoundVariants: [
      { variant: "solid", tone: "danger", class: "bg-danger text-danger-foreground hover:bg-danger" },
      { variant: "outline", tone: "danger", class: "border-danger text-danger" },
    ],
    defaultVariants: { variant: "solid", tone: "brand", size: "md" },
  }
);

export const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, tone, size, loading, disabled, children, ...props }, ref) => (
    <button
      ref={ref}
      className={cn(buttonVariants({ variant, tone, size }), className)}
      disabled={disabled || loading}
      {...props}
    >
      {loading && <Loader2 className="size-4 animate-spin" aria-hidden />}
      {children}
    </button>
  )
);
Button.displayName = "Button";
```

- [ ] **Step 3: index.ts** `export { Button, buttonVariants } from "./button"; export type { ButtonProps } from "./button.types";`

- [ ] **Step 4: button.test.tsx**
```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import { buttonVariants } from "./button";
describe("buttonVariants", () => {
  it("default = solid brand md", () => {
    const c = buttonVariants({});
    expect(c).toContain("bg-primary");
    expect(c).toContain("h-10");
  });
  it("danger solid swaps to danger bg", () => {
    expect(buttonVariants({ variant: "solid", tone: "danger" })).toContain("bg-danger");
  });
});
```

- [ ] **Step 5: button.showcase.tsx（四件套展示规格）**
```tsx
import type { ShowcaseSpec } from "../showcase/types";
import { Button } from "./button";
export const buttonShowcase: ShowcaseSpec = {
  controls: [
    { prop: "variant", type: "select", options: ["solid", "outline", "ghost"], defaultValue: "solid" },
    { prop: "tone", type: "select", options: ["brand", "danger"], defaultValue: "brand" },
    { prop: "size", type: "select", options: ["sm", "md", "lg"], defaultValue: "md" },
    { prop: "loading", type: "boolean", defaultValue: false },
    { prop: "children", type: "text", defaultValue: "瑚琏按钮" },
  ],
  states: [
    { name: "default", render: () => <Button>默认</Button> },
    { name: "outline", render: () => <Button variant="outline">描边</Button> },
    { name: "ghost", render: () => <Button variant="ghost">幽灵</Button> },
    { name: "danger", render: () => <Button tone="danger">危险</Button> },
    { name: "disabled", render: () => <Button disabled>禁用</Button> },
    { name: "loading", render: () => <Button loading>加载中</Button> },
  ],
  renderWithProps: (p) => <Button {...(p as any)}>{p.children as string}</Button>,
  toCode: (p) =>
    `<Button variant="${p.variant}" tone="${p.tone}" size="${p.size}"${p.loading ? " loading" : ""}>${p.children}</Button>`,
};
```

- [ ] **Step 6: Run** `pnpm --filter @hulianui/ui test`. Expected: PASS.
- [ ] **Step 7: Commit** `git add -A && git commit -m "feat(ui): Button(CVA 变体+语义 token+loading)"`

---

### Task 6: Switch（Base UI 受控 + ARIA + 焦点环）

**Files:** Create `packages/ui/src/switch/{switch.tsx,switch.types.ts,switch.showcase.tsx,index.ts}`

> 验证 Base UI 受控状态 + ARIA + 焦点环。Base UI Switch API：`Switch.Root` + `Switch.Thumb`。

- [ ] **Step 1: switch.types.ts**
```ts
export interface SwitchProps {
  checked?: boolean;
  defaultChecked?: boolean;
  onCheckedChange?: (checked: boolean) => void;
  disabled?: boolean;
  id?: string;
  "aria-label"?: string;
}
```

- [ ] **Step 2: switch.tsx**
```tsx
"use client";
import { Switch as BaseSwitch } from "@base-ui-components/react/switch";
import { cn } from "../lib/cn";
import type { SwitchProps } from "./switch.types";

export function Switch({ className, ...props }: SwitchProps & { className?: string }) {
  return (
    <BaseSwitch.Root
      {...props}
      className={cn(
        "relative inline-flex h-6 w-10 items-center rounded-full border border-border transition-colors outline-none",
        "bg-surface-hover data-[checked]:bg-primary",
        "focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-bg",
        "disabled:opacity-50 disabled:pointer-events-none",
        className
      )}
    >
      <BaseSwitch.Thumb className="block size-5 translate-x-0.5 rounded-full bg-surface shadow transition-transform data-[checked]:translate-x-[1.125rem]" />
    </BaseSwitch.Root>
  );
}
```

- [ ] **Step 3: index.ts** `export { Switch } from "./switch"; export type { SwitchProps } from "./switch.types";`

- [ ] **Step 4: switch.showcase.tsx**
```tsx
import { useState } from "react";
import type { ShowcaseSpec } from "../showcase/types";
import { Switch } from "./switch";
function Controlled(p: Record<string, unknown>) {
  const [on, setOn] = useState(false);
  return <Switch checked={on} onCheckedChange={setOn} disabled={p.disabled as boolean} aria-label="demo" />;
}
export const switchShowcase: ShowcaseSpec = {
  controls: [{ prop: "disabled", type: "boolean", defaultValue: false }],
  states: [
    { name: "off", render: () => <Switch aria-label="off" /> },
    { name: "on", render: () => <Switch defaultChecked aria-label="on" /> },
    { name: "disabled", render: () => <Switch disabled aria-label="disabled" /> },
    { name: "disabled-on", render: () => <Switch disabled defaultChecked aria-label="disabled-on" /> },
  ],
  renderWithProps: (p) => <Controlled {...p} />,
  toCode: (p) => `<Switch${p.disabled ? " disabled" : ""} />`,
};
```

- [ ] **Step 5: typecheck** `pnpm --filter @hulianui/ui typecheck`. Expected: 通过（若 Base UI Switch API 与上不符，按 `node_modules/@base-ui-components/react` 实际导出修正）。
- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(ui): Switch(Base UI 受控+ARIA+焦点环)"`

---

### Task 7: Dialog（Base UI Portal + focus trap）

**Files:** Create `packages/ui/src/dialog/{dialog.tsx,dialog.types.ts,dialog.showcase.tsx,index.ts}`

> 命脉验证：Portal + focus trap + Esc/遮罩关闭 + 焦点归还。Base UI Dialog API：`Dialog.Root/Trigger/Portal/Backdrop/Popup/Title/Description/Close`。

- [ ] **Step 1: dialog.tsx（薄封装，组合 Base UI parts，皮肤走语义 token）**
```tsx
"use client";
import { Dialog as BaseDialog } from "@base-ui-components/react/dialog";
import type { ReactNode } from "react";
import { cn } from "../lib/cn";

export function Dialog({ children, ...props }: React.ComponentProps<typeof BaseDialog.Root>) {
  return <BaseDialog.Root {...props}>{children}</BaseDialog.Root>;
}
export const DialogTrigger = BaseDialog.Trigger;
export const DialogClose = BaseDialog.Close;

export function DialogContent({ title, description, children, className }:
  { title: string; description?: string; children?: ReactNode; className?: string }) {
  return (
    <BaseDialog.Portal>
      <BaseDialog.Backdrop className="fixed inset-0 bg-black/40 backdrop-blur-sm data-[starting-style]:opacity-0 data-[ending-style]:opacity-0 transition-opacity" />
      <BaseDialog.Popup
        className={cn(
          "fixed left-1/2 top-1/2 w-[min(90vw,28rem)] -translate-x-1/2 -translate-y-1/2 rounded-[var(--radius)] border border-border bg-surface p-6 text-foreground shadow-xl outline-none",
          "data-[starting-style]:scale-95 data-[starting-style]:opacity-0 data-[ending-style]:scale-95 data-[ending-style]:opacity-0 transition-all",
          className
        )}
      >
        <BaseDialog.Title className="text-lg font-semibold">{title}</BaseDialog.Title>
        {description && <BaseDialog.Description className="mt-1 text-sm text-muted">{description}</BaseDialog.Description>}
        <div className="mt-4">{children}</div>
      </BaseDialog.Popup>
    </BaseDialog.Portal>
  );
}
```

- [ ] **Step 2: index.ts** `export { Dialog, DialogTrigger, DialogClose, DialogContent } from "./dialog";`

- [ ] **Step 3: dialog.showcase.tsx**
```tsx
import type { ShowcaseSpec } from "../showcase/types";
import { Dialog, DialogTrigger, DialogClose, DialogContent } from "./dialog";
import { Button } from "../button/button";
function Demo() {
  return (
    <Dialog>
      <DialogTrigger render={<Button variant="outline">打开对话框</Button>} />
      <DialogContent title="瑚琏对话框" description="Portal + focus trap 验证：Tab 不出框，Esc 关闭，焦点归还触发按钮。">
        <div className="flex justify-end gap-2">
          <DialogClose render={<Button variant="ghost">取消</Button>} />
          <DialogClose render={<Button>确定</Button>} />
        </div>
      </DialogContent>
    </Dialog>
  );
}
export const dialogShowcase: ShowcaseSpec = {
  controls: [],
  states: [{ name: "default", render: () => <Demo /> }],
  renderWithProps: () => <Demo />,
  toCode: () => `<Dialog>\n  <DialogTrigger render={<Button>打开</Button>} />\n  <DialogContent title="标题">…</DialogContent>\n</Dialog>`,
};
```

- [ ] **Step 4: src/index.ts 桶导出全部**
```ts
export * from "./button";
export * from "./switch";
export * from "./dialog";
export { ThemeProvider } from "./theme/theme-provider";
export { useTheme } from "./theme/use-theme";
export type { Theme, ThemeSetting } from "./theme/use-theme";
export { cn } from "./lib/cn";
export type { ShowcaseSpec, Control, StateSpec } from "./showcase/types";
export { buttonShowcase } from "./button/button.showcase";
export { switchShowcase } from "./switch/switch.showcase";
export { dialogShowcase } from "./dialog/dialog.showcase";
```

- [ ] **Step 5: typecheck** `pnpm --filter @hulianui/ui typecheck`. Expected: 通过（Base UI `render` prop 合并 API 若不符以实际版本修正）。
- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(ui): Dialog(Base UI Portal+focus trap) + 桶导出"`

---

### Task 8: apps/www — Next.js 站 + dogfood 模型 A + anti-FOUC + 三组件渲染

**Files:** Create `apps/www/{package.json,next.config.mjs,tsconfig.json,postcss.config.mjs,app/globals.css,app/layout.tsx,app/theme-script.tsx,app/page.tsx,components/theme-toggle.tsx}`

- [ ] **Step 1: package.json（端口写死 5512）**
```json
{
  "name": "www", "private": true,
  "scripts": {
    "dev": "next dev -p 5512",
    "build": "next build",
    "start": "next start -p 5512",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^15.0.0", "react": "^18.3.0", "react-dom": "^18.3.0",
    "@hulianui/ui": "workspace:*", "@hulianui/tokens": "workspace:*",
    "@hulianui/mocks": "workspace:*",
    "@base-ui-components/react": "latest", "lucide-react": "^0.400.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0", "@tailwindcss/postcss": "^4.0.0",
    "typescript": "^5.5.0", "@types/react": "^18.3.0", "@types/node": "^22.0.0",
    "@next/mdx": "^15.0.0", "@mdx-js/react": "^3.0.0"
  }
}
```

- [ ] **Step 2: postcss.config.mjs** `export default { plugins: { "@tailwindcss/postcss": {} } };`

- [ ] **Step 3: globals.css（模型 A 接入：tokens 在前 + preset + @source 扫库源码）**
```css
@import "@hulianui/tokens/tokens.css";
@import "@hulianui/tokens/preset.css";
@source "../../../packages/ui/src/**/*.{ts,tsx}";
html, body { background: var(--color-bg); color: var(--color-foreground); }
body { font-family: ui-sans-serif, system-ui, sans-serif; }
```
> `@source` 让消费方 Tailwind 扫到 `@hulianui/ui` 源码里的 className（模型 A 核心）。

- [ ] **Step 4: theme-script.tsx（anti-FOUC inline script，归 www 不归库）**
```tsx
export const themeScript = `(function(){try{var s=localStorage.getItem('hulian-theme')||'system';var d=s==='dark'||(s==='system'&&matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.setAttribute('data-theme',d?'dark':'light');}catch(e){}})();`;
```

- [ ] **Step 5: layout.tsx（tokens 最前 + inline script 在 head + ThemeProvider）**
```tsx
import type { ReactNode } from "react";
import "./globals.css";
import { ThemeProvider } from "@hulianui/ui";
import { themeScript } from "./theme-script";
export const metadata = { title: "瑚琏 Hulian", description: "颜值 + 好用的 React 设计系统" };
export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="zh-CN" suppressHydrationWarning>
      <head><script dangerouslySetInnerHTML={{ __html: themeScript }} /></head>
      <body><ThemeProvider defaultSetting="system">{children}</ThemeProvider></body>
    </html>
  );
}
```

- [ ] **Step 6: theme-toggle.tsx**
```tsx
"use client";
import { useTheme } from "@hulianui/ui";
import { Moon, Sun } from "lucide-react";
export function ThemeToggle() {
  const { theme, toggle } = useTheme();
  return (
    <button onClick={toggle} aria-label="切换明暗主题"
      className="inline-flex size-9 items-center justify-center rounded-[var(--radius)] border border-border bg-surface text-foreground hover:bg-surface-hover">
      {theme === "dark" ? <Sun className="size-4" /> : <Moon className="size-4" />}
    </button>
  );
}
```

- [ ] **Step 7: page.tsx（首页渲染三组件，证明 dogfood + 换肤）**
```tsx
import { Button, Switch, Dialog, DialogTrigger, DialogClose, DialogContent } from "@hulianui/ui";
import { ThemeToggle } from "../components/theme-toggle";
export default function Home() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16 space-y-10">
      <header className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold">瑚琏 Hulian</h1>
        <ThemeToggle />
      </header>
      <section className="space-y-4">
        <div className="flex flex-wrap gap-3">
          <Button>默认</Button>
          <Button variant="outline">描边</Button>
          <Button variant="ghost">幽灵</Button>
          <Button tone="danger">危险</Button>
          <Button loading>加载</Button>
        </div>
        <div className="flex items-center gap-3"><Switch defaultChecked aria-label="demo" /><span className="text-muted">开关</span></div>
        <Dialog>
          <DialogTrigger render={<Button variant="outline">打开对话框</Button>} />
          <DialogContent title="瑚琏对话框" description="Portal + focus trap 验证。">
            <div className="flex justify-end gap-2">
              <DialogClose render={<Button variant="ghost">取消</Button>} />
              <DialogClose render={<Button>确定</Button>} />
            </div>
          </DialogContent>
        </Dialog>
      </section>
    </main>
  );
}
```

- [ ] **Step 8: tsconfig.json** 继承 base，加 Next 插件：
```json
{ "extends": "../../tsconfig.base.json",
  "compilerOptions": { "plugins": [{ "name": "next" }], "paths": {} },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"] }
```

- [ ] **Step 9: 安装 + 起站验证** Run: `pnpm install`，then `pnpm --filter www dev`（后台）。用 Playwright/Chrome 打开 `http://localhost:5512`，截图 light + 切 dark 截图。Expected: 三组件渲染正确、换肤 0 闪烁、Dialog 能开关且焦点受困。
- [ ] **Step 10: anti-FOUC 验收** Slow 4G 节流硬刷，抓 first paint：确认首帧即正确底色无白闪。
- [ ] **Step 11: Commit** `git add -A && git commit -m "feat(www): Next 站点(5512)+dogfood 模型A+anti-FOUC+三组件渲染"`

**P0 验收门：** 上述截图证明切主题 0 闪烁、三组件跑起来、Dialog 的 Portal+focus trap 通过 → Base UI 命脉验证完成。

---

## P1 — 展示基建

### Task 9: @hulianui/mocks（faker 工厂 + MSW handlers）

**Files:** Create `packages/mocks/{package.json,src/factories.ts,src/handlers.ts,src/browser.ts,src/index.ts}`

- [ ] **Step 1: package.json**
```json
{ "name": "@hulianui/mocks", "version": "0.0.0", "type": "module",
  "exports": { ".": "./src/index.ts", "./browser": "./src/browser.ts" },
  "dependencies": { "@faker-js/faker": "^9.0.0", "msw": "^2.4.0" } }
```

- [ ] **Step 2: factories.ts（确定性种子，避免 hydration mismatch）**
```ts
import { faker } from "@faker-js/faker";
export interface DemoUser { id: string; name: string; email: string; role: string; avatar: string; }
export function makeUsers(count = 24, seed = 42): DemoUser[] {
  faker.seed(seed);
  return Array.from({ length: count }, () => ({
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    email: faker.internet.email(),
    role: faker.helpers.arrayElement(["管理员", "编辑", "访客"]),
    avatar: faker.image.avatarGitHub(),
  }));
}
```

- [ ] **Step 3: handlers.ts（分页 API，供异步 demo）**
```ts
import { http, HttpResponse } from "msw";
import { makeUsers } from "./factories";
const ALL = makeUsers(60);
export const handlers = [
  http.get("/api/users", ({ request }) => {
    const url = new URL(request.url);
    const page = Number(url.searchParams.get("page") ?? "1");
    const size = 8;
    const start = (page - 1) * size;
    return HttpResponse.json({ total: ALL.length, page, items: ALL.slice(start, start + size) });
  }),
];
```

- [ ] **Step 4: browser.ts + index.ts**
```ts
// browser.ts
import { setupWorker } from "msw/browser";
import { handlers } from "./handlers";
export const worker = setupWorker(...handlers);
// index.ts
export * from "./factories";
export { handlers } from "./handlers";
```

- [ ] **Step 5: 生成 MSW worker 脚本到 www public** Run: `cd apps/www && pnpm exec msw init public/ --save` (or copy mockServiceWorker.js)。Expected: `apps/www/public/mockServiceWorker.js` 存在。
- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(mocks): faker 工厂 + MSW 分页 handlers + worker"`

---

### Task 10: MSW Provider（App Router 客户端启动，避开注册时机坑）

**Files:** Create `apps/www/components/msw-provider.tsx`; modify `apps/www/app/layout.tsx`

> App Router 坑：worker 只能在 client、且 await ready 后再渲染依赖数据的子树，否则首个请求漏过拦截。

- [ ] **Step 1: msw-provider.tsx**
```tsx
"use client";
import { useEffect, useState, type ReactNode } from "react";
export function MswProvider({ children }: { children: ReactNode }) {
  const [ready, setReady] = useState(process.env.NODE_ENV === "production");
  useEffect(() => {
    if (process.env.NODE_ENV === "production") return;
    let active = true;
    import("@hulianui/mocks/browser").then(async ({ worker }) => {
      await worker.start({ onUnhandledRequest: "bypass", quiet: true });
      if (active) setReady(true);
    });
    return () => { active = false; };
  }, []);
  if (!ready) return null;
  return <>{children}</>;
}
```
> 注：生产环境不启 MSW（直接 ready=true 透传）。开发态 await ready 后再渲染。

- [ ] **Step 2: 包裹 layout**：在 `ThemeProvider` 内层包 `MswProvider`。
- [ ] **Step 3: typecheck + 起站** Run: `pnpm --filter www dev`，Console 应见 "[MSW] Mocking enabled"。
- [ ] **Step 4: Commit** `git add -A && git commit -m "feat(www): MSW Provider(App Router 客户端启动)"`

---

### Task 11: 展示三件套 — ComponentPreview / StatesGallery / Playground

**Files:** Create `apps/www/components/showcase/{component-preview.tsx,states-gallery.tsx,playground.tsx}`

- [ ] **Step 1: component-preview.tsx（实时 demo + 代码 Tab）**
```tsx
"use client";
import { useState, type ReactNode } from "react";
export function ComponentPreview({ children, code }: { children: ReactNode; code: string }) {
  const [tab, setTab] = useState<"preview" | "code">("preview");
  return (
    <div className="rounded-[var(--radius)] border border-border overflow-hidden">
      <div className="flex gap-1 border-b border-border bg-surface px-2 py-1.5 text-sm">
        {(["preview", "code"] as const).map((t) => (
          <button key={t} onClick={() => setTab(t)}
            className={`rounded px-2 py-1 ${tab === t ? "bg-surface-hover text-foreground" : "text-muted"}`}>
            {t === "preview" ? "预览" : "代码"}
          </button>
        ))}
      </div>
      {tab === "preview"
        ? <div className="flex flex-wrap items-center gap-4 bg-bg p-8">{children}</div>
        : <pre className="overflow-auto bg-surface p-4 text-sm"><code>{code}</code></pre>}
    </div>
  );
}
```

- [ ] **Step 2: states-gallery.tsx**
```tsx
import type { StateSpec } from "@hulianui/ui";
export function StatesGallery({ states }: { states: StateSpec[] }) {
  return (
    <div className="grid grid-cols-[repeat(auto-fill,minmax(8rem,1fr))] gap-4">
      {states.map((s) => (
        <div key={s.name} className="flex flex-col items-center gap-2 rounded-[var(--radius)] border border-border bg-surface p-4">
          <div className="flex min-h-12 items-center justify-center">{s.render()}</div>
          <span className="text-xs text-muted">{s.name}</span>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: playground.tsx（手写 schema → controls + live code）**
```tsx
"use client";
import { useState } from "react";
import type { ShowcaseSpec } from "@hulianui/ui";
export function Playground({ spec }: { spec: ShowcaseSpec }) {
  const [props, setProps] = useState<Record<string, unknown>>(
    Object.fromEntries(spec.controls.map((c) => [c.prop, c.defaultValue]))
  );
  const set = (k: string, v: unknown) => setProps((p) => ({ ...p, [k]: v }));
  return (
    <div className="grid gap-4 md:grid-cols-[1fr,16rem]">
      <div className="space-y-4">
        <div className="flex min-h-32 items-center justify-center rounded-[var(--radius)] border border-border bg-bg p-8">
          {spec.renderWithProps(props)}
        </div>
        <pre className="overflow-auto rounded-[var(--radius)] bg-surface p-4 text-sm"><code>{spec.toCode(props)}</code></pre>
      </div>
      <div className="space-y-3 rounded-[var(--radius)] border border-border bg-surface p-4">
        {spec.controls.map((c) => (
          <label key={c.prop} className="block text-sm">
            <span className="mb-1 block text-muted">{c.label ?? c.prop}</span>
            {c.type === "select" && (
              <select className="w-full rounded border border-border bg-bg px-2 py-1"
                value={String(props[c.prop])} onChange={(e) => set(c.prop, e.target.value)}>
                {c.options!.map((o) => <option key={o} value={o}>{o}</option>)}
              </select>
            )}
            {c.type === "boolean" && (
              <input type="checkbox" checked={Boolean(props[c.prop])} onChange={(e) => set(c.prop, e.target.checked)} />
            )}
            {c.type === "text" && (
              <input className="w-full rounded border border-border bg-bg px-2 py-1"
                value={String(props[c.prop])} onChange={(e) => set(c.prop, e.target.value)} />
            )}
            {c.type === "number" && (
              <input type="number" className="w-full rounded border border-border bg-bg px-2 py-1"
                value={Number(props[c.prop])} onChange={(e) => set(c.prop, Number(e.target.value))} />
            )}
          </label>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: typecheck** `pnpm --filter www typecheck`. Expected: 通过。
- [ ] **Step 5: Commit** `git add -A && git commit -m "feat(www): 展示三件套(ComponentPreview/StatesGallery/Playground)"`

---

### Task 12: 组件展示页 + 真实数据 + MSW 异步 demo（四 mock 全亮）

**Files:** Create `apps/www/app/components/page.tsx`, `apps/www/components/showcase/sample-table.tsx`, `apps/www/components/showcase/async-users.tsx`; 组件页拼装

- [ ] **Step 1: sample-table.tsx（mock①真实样例数据：faker 表格）**
```tsx
import { makeUsers } from "@hulianui/mocks";
export function SampleTable() {
  const users = makeUsers(8);
  return (
    <table className="w-full text-sm">
      <thead className="text-muted"><tr><th className="p-2 text-left">姓名</th><th className="p-2 text-left">邮箱</th><th className="p-2 text-left">角色</th></tr></thead>
      <tbody>
        {users.map((u) => (
          <tr key={u.id} className="border-t border-border">
            <td className="flex items-center gap-2 p-2"><img src={u.avatar} alt="" className="size-6 rounded-full" />{u.name}</td>
            <td className="p-2 text-muted">{u.email}</td><td className="p-2">{u.role}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

- [ ] **Step 2: async-users.tsx（mock③ MSW 异步加载+分页，Button loading 联动）**
```tsx
"use client";
import { useEffect, useState } from "react";
import { Button } from "@hulianui/ui";
import type { DemoUser } from "@hulianui/mocks";
export function AsyncUsers() {
  const [page, setPage] = useState(1);
  const [data, setData] = useState<{ items: DemoUser[]; total: number } | null>(null);
  const [loading, setLoading] = useState(false);
  useEffect(() => {
    setLoading(true);
    fetch(`/api/users?page=${page}`).then((r) => r.json()).then((d) => { setData(d); setLoading(false); });
  }, [page]);
  return (
    <div className="space-y-3">
      <ul className="min-h-48 space-y-1 text-sm">
        {data?.items.map((u) => <li key={u.id} className="rounded bg-surface px-3 py-1.5">{u.name} · <span className="text-muted">{u.role}</span></li>)}
      </ul>
      <div className="flex items-center gap-3">
        <Button size="sm" variant="outline" disabled={page === 1 || loading} onClick={() => setPage((p) => p - 1)}>上一页</Button>
        <span className="text-sm text-muted">第 {page} 页 / 共 {data ? Math.ceil(data.total / 8) : "…"} 页</span>
        <Button size="sm" variant="outline" loading={loading} disabled={!data || page >= Math.ceil(data.total / 8)} onClick={() => setPage((p) => p + 1)}>下一页</Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: components/page.tsx（把四 mock 拼到三组件页）**
组合：每个组件一节，含 `<ComponentPreview>`（demo+code）、`<StatesGallery states={spec.states}/>`、`<Playground spec={spec}/>`；页面底部放 `<SampleTable/>`（mock①）+ `<AsyncUsers/>`（mock③）。顶部 `<ThemeToggle/>`。导入 `buttonShowcase/switchShowcase/dialogShowcase`。

```tsx
import { buttonShowcase, switchShowcase, dialogShowcase, type ShowcaseSpec } from "@hulianui/ui";
import { ComponentPreview } from "../../components/showcase/component-preview";
import { StatesGallery } from "../../components/showcase/states-gallery";
import { Playground } from "../../components/showcase/playground";
import { SampleTable } from "../../components/showcase/sample-table";
import { AsyncUsers } from "../../components/showcase/async-users";
import { ThemeToggle } from "../../components/theme-toggle";

const SPECS: { name: string; spec: ShowcaseSpec }[] = [
  { name: "Button", spec: buttonShowcase },
  { name: "Switch", spec: switchShowcase },
  { name: "Dialog", spec: dialogShowcase },
];
export default function ComponentsPage() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-12 space-y-16">
      <header className="flex items-center justify-between"><h1 className="text-2xl font-semibold">组件</h1><ThemeToggle /></header>
      {SPECS.map(({ name, spec }) => (
        <section key={name} className="space-y-6">
          <h2 className="text-xl font-semibold">{name}</h2>
          <ComponentPreview code={spec.toCode(Object.fromEntries(spec.controls.map((c) => [c.prop, c.defaultValue])))}>
            {spec.states[0].render()}
          </ComponentPreview>
          <div><h3 className="mb-3 text-sm font-medium text-muted">全状态</h3><StatesGallery states={spec.states} /></div>
          {spec.controls.length > 0 && <div><h3 className="mb-3 text-sm font-medium text-muted">Playground</h3><Playground spec={spec} /></div>}
        </section>
      ))}
      <section className="space-y-6">
        <h2 className="text-xl font-semibold">真实样例数据（faker）</h2>
        <div className="rounded-[var(--radius)] border border-border p-4"><SampleTable /></div>
        <h2 className="text-xl font-semibold">异步加载 + 分页（MSW）</h2>
        <div className="rounded-[var(--radius)] border border-border p-4"><AsyncUsers /></div>
      </section>
    </main>
  );
}
```

- [ ] **Step 4: 起站验证四 mock** Run: dev 站点 `http://localhost:5512/components`。Playwright 截图 light/dark。Expected：①faker 表格有头像数据 ②StatesGallery 全状态铺开 ③AsyncUsers 点下一页 loading→换页 ④Playground 改 variant/文案实时变 + 代码同步。
- [ ] **Step 5: Commit** `git add -A && git commit -m "feat(www): 组件展示页四 mock 全亮(真实数据/全状态/MSW/playground)"`

---

### Task 13: 收口验证 + README

**Files:** Create `README.md`; run full gates

- [ ] **Step 1: 全量 typecheck + test + build** Run: `pnpm typecheck && pnpm test && pnpm --filter www build`. Expected: 全绿。
- [ ] **Step 2: 验收对照 spec §9 五条**，逐条用截图/命令证明：① 5512 不撞口 ② 0 闪烁(节流首帧截图) ③ 三组件行为(Dialog 键盘 Tab 受困、Esc 关、焦点归还) ④ 四 mock 全亮 ⑤ import 链路(www 即 dogfood 消费者，样式+类型 OK)。
- [ ] **Step 3: README** 写项目简介、信条、`pnpm dev` 起站(5512)、目录结构、接入三步(模型 A)。
- [ ] **Step 4: Commit** `git add -A && git commit -m "docs: README + P0/P1 收口验证"`

---

## Self-Review 结论（写计划时已自查）

- **Spec 覆盖**：§3A 分发模型→Task2/3/8 globals.css @source；§5 主题接缝→Task4(库)+Task8(www script)；§6 四件套+手写schema→Task5-7+showcase/types；§7 四 mock→Task9-12；§8 三标杆→Task5-7；§9 验收→Task13。无遗漏。
- **类型一致**：`ShowcaseSpec/Control/StateSpec` 在 Task3 定义，Task5-7、11-12 一致引用。`toCode/renderWithProps/states/controls` 字段贯穿一致。
- **Base UI 风险点**：Switch/Dialog 的 Base UI API（`Switch.Root/Thumb`、`Dialog.Root/Trigger/Portal/Backdrop/Popup/Title/Description/Close`、`render` prop 合并）以安装后 `node_modules/@base-ui-components/react` 实际导出为准，Task6/7 已标注若不符则按实际修正。
- **已知坑挂钩**：Tailwind v4 dark variant(Task2 `@custom-variant`)、MSW App Router 注册时机(Task10 await ready)、faker 确定性种子防 hydration mismatch(Task9 `faker.seed`)。
