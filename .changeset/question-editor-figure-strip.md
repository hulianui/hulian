---
"@hulianui/ui": minor
---

feat(question-editor): 题图缩略图条此前只有「缩略图 + 删除 + 插入图片」，从自造表单迁过来的消费方丢了三件本来有的能力；现在补齐：每张图底下一对「前移 / 后移」（题图顺序就是它们写在题干末尾的顺序，也就是组卷预览、学生端与 docx 导出里的显示顺序，此前要调序只能删掉重传一遍），点缩略图用 `ImageViewer` 打开看大图（80px 够认出是哪张，认不出几何题图里的字母标注），上传失败那一行留住原来的 `File` 并给出「重试」（此前只能关掉提示重新选文件）。调序照常经 `onChange` 回传整份 `Question`，不另开 `onSortFigures`：顺序本来就是 `stem` 的一部分，编辑器手里既有题干也有 `figureFilter`，多一个口子只会让题图顺序有两个真源；用按钮而不是拖拽，与本编辑器里选项调序同一套交互，键盘与读屏天然可用，也不必为一条缩略图条把 dnd-kit 塞给所有 `@hulianui/ui/math` 消费方。类型 / 大小 / 张数上限仍由消费方在 `onUploadFigure` 里 `throw` (#354)
