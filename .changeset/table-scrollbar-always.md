---
"@hulianui/ui": patch
---

Table 新增 `scrollbar="always"`：外壳套用与 `stickyScrollbar` 代理条同一份经典滚动条皮肤，macOS overlay 滚动条下宽表也能一眼看出右边还有列；外壳恒按真实滚动态打 `data-overflow-left` / `data-overflow-right`，冻结列分隔阴影只在对应侧还有内容时出现。
