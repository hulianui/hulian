---
"@hulianui/ui": minor
---

feat(page-header): `meta` 此前恒定渲染成标题行下面那条 `<ul>`，位置不可选。只有两三个短事实值时（`沪科技版` · `4 章` · `0 题` · `原件 105MB`）那一整行几乎是空的，白白把页头拉高一档；而这些值本来就与 `subTitle` 同字号同色，视觉上就是一串。新增 `metaPlacement`（`"block"` 默认 / `"inline"`）：内联档把这条 `<ul>` 挂进标题群、去掉 `mt-2`，排在 `subTitle` 之后、`tags` 之前（同一串弱化小字不被状态标记劈开），有 `subTitle` 时在它与第一项之间补一个同款分隔符——否则第一处是空格、后面全是中点，看着像漏写；没有 `subTitle` 则不补，标题是另一档字号字重，紧跟一个中点会读成「标题也是一项」。两档语义完全一样：仍是 `ul`/`li`、仍自动跳过空项、分隔符仍是 `aria-hidden` 装饰位。这条是 #240 的延续：那次解决「有没有 meta」，这次解决「放哪」，消费方不必再为了排成一行把版本名和事实值 `join(" · ")` 塞进 `subTitle`——一拼成文本，列表语义、空项跳过、装饰位分隔符就全丢了 (#357)
