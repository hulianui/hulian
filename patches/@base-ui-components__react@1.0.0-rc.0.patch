diff --git a/esm/toast/root/ToastRoot.js b/esm/toast/root/ToastRoot.js
index 7a4b511950f49aa394687c7e381e1ea6f9ae8699..0c875a5a41085de3215ab15c248556c776b14503 100644
--- a/esm/toast/root/ToastRoot.js
+++ b/esm/toast/root/ToastRoot.js
@@ -146,7 +146,7 @@ export const ToastRoot = /*#__PURE__*/React.forwardRef(function ToastRoot(compon
       }
     }
   });
-  const recalculateHeight = useStableCallback(() => {
+  const recalculateHeight = useStableCallback((shouldFlushSync = false) => {
     const element = rootRef.current;
     if (!element) {
       return;
@@ -155,14 +155,26 @@ export const ToastRoot = /*#__PURE__*/React.forwardRef(function ToastRoot(compon
     element.style.height = 'auto';
     const height = element.offsetHeight;
     element.style.height = previousHeight;
-    ReactDOM.flushSync(() => {
+    // hulian patch (base-ui rc.0): flushSync inside a layout effect is redundant and
+    // illegal under React 19 ("flushSync was called from inside a lifecycle method").
+    // Layout-effect callers (this mount effect + ToastContent mount effect) pass no arg
+    // -> plain update(), which React already flushes synchronously before paint.
+    // Resize/Mutation observer callers pass entries (truthy) -> flushSync, and run
+    // outside render so it's safe and still avoids stacking flicker. Mirrors upstream
+    // mui/base-ui master fix (recalculateHeight(flushSync = false)).
+    const update = () => {
       setToasts(prev => prev.map(t => t.id === toast.id ? {
         ...t,
         ref: rootRef,
         height,
         transitionStatus: undefined
       } : t));
-    });
+    };
+    if (shouldFlushSync) {
+      ReactDOM.flushSync(update);
+    } else {
+      update();
+    }
   });
   useIsoLayoutEffect(recalculateHeight, [recalculateHeight]);
   function applyDirectionalDamping(deltaX, deltaY) {
diff --git a/toast/root/ToastRoot.js b/toast/root/ToastRoot.js
index 5d887553ccab90f6d633e9b39d6128c9b2e521a9..c0e96aebd255151e03ed3a12e6d06d1f472e794c 100644
--- a/toast/root/ToastRoot.js
+++ b/toast/root/ToastRoot.js
@@ -152,7 +152,7 @@ const ToastRoot = exports.ToastRoot = /*#__PURE__*/React.forwardRef(function Toa
       }
     }
   });
-  const recalculateHeight = (0, _useStableCallback.useStableCallback)(() => {
+  const recalculateHeight = (0, _useStableCallback.useStableCallback)((shouldFlushSync = false) => {
     const element = rootRef.current;
     if (!element) {
       return;
@@ -161,14 +161,26 @@ const ToastRoot = exports.ToastRoot = /*#__PURE__*/React.forwardRef(function Toa
     element.style.height = 'auto';
     const height = element.offsetHeight;
     element.style.height = previousHeight;
-    ReactDOM.flushSync(() => {
+    // hulian patch (base-ui rc.0): flushSync inside a layout effect is redundant and
+    // illegal under React 19 ("flushSync was called from inside a lifecycle method").
+    // Layout-effect callers (this mount effect + ToastContent mount effect) pass no arg
+    // -> plain update(), which React already flushes synchronously before paint.
+    // Resize/Mutation observer callers pass entries (truthy) -> flushSync, and run
+    // outside render so it's safe and still avoids stacking flicker. Mirrors upstream
+    // mui/base-ui master fix (recalculateHeight(flushSync = false)).
+    const update = () => {
       setToasts(prev => prev.map(t => t.id === toast.id ? {
         ...t,
         ref: rootRef,
         height,
         transitionStatus: undefined
       } : t));
-    });
+    };
+    if (shouldFlushSync) {
+      ReactDOM.flushSync(update);
+    } else {
+      update();
+    }
   });
   (0, _useIsoLayoutEffect.useIsoLayoutEffect)(recalculateHeight, [recalculateHeight]);
   function applyDirectionalDamping(deltaX, deltaY) {
