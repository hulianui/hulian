# Cloudflare Pages 重定向表（构建后落在 out/_redirects）。
#
# 英文站原本挂在 /en/*，现已搬到站点根：品牌词 hulianui / hulian ui 是英文查询，
# 落地页必须是根域上的英文页；中文站改挂 /zh/*。
#
# 旧的 /en/* 地址被 hreflang 指向过、也已进入搜索引擎索引，不 301 就会集体变成 404，
# 把此前积累的收录全部丢掉。中文侧不需要对应规则 —— 那些 URL 仍然存在，只是内容
# 由中文换成了英文（内容替换，不是地址迁移）。
/en/*  /:splat  301
/en    /        301
